import React from 'react';
import { Grid, Typography, Link } from '@material-ui/core';

const Index = () => (
  <Grid
    container
    direction="column"
    justify="center"
    // alignItems="center"
    spacing={2}
  >
    <Grid item>
      <Typography variant="h6">Poincare disks</Typography>
    </Grid>
  </Grid>
);

export default Index;
